#include "main.h"

/**
 * mul - multipies 2 integers
 * @a: an argument to be multiplied
 * @b: the 2nd argument to be multiplied
 * Return: result of the multiplied values
 */
int mul(int a, int b)
{
	return (a * b);
}
